<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techonehub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechOneHub">
        <activity android:name=".CadastrarServicoActivity">
        </activity>
        <activity android:name=".ServicoActivity" />
        <activity android:name=".ConsultarClienteActivity" />
        <activity android:name=".CadastrarClienteActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".AgendaActivity">
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>